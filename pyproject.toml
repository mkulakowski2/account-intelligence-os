[project]
name = "account-intelligence-os"
version = "0.1.0"
description = "Open-source account intelligence agents and decision intelligence primitives"
requires-python = ">=3.9"
dependencies = ["jsonschema>=4.0.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]

[project.scripts]
account-intel = "account_intel.run:main"
